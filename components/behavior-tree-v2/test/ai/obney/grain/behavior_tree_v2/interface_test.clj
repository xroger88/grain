(ns ai.obney.grain.behavior-tree-v2.interface-test
  (:require [clojure.test :as test :refer :all]
            [ai.obney.grain.behavior-tree-v2.interface :as behavior-tree-v2]))

(deftest dummy-test
  (is (= 1 1)))

