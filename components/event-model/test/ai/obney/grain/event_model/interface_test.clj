(ns ai.obney.grain.event-model.interface-test
  (:require [clojure.test :as test :refer :all]
            [ai.obney.grain.event-model.interface :as event-model]))

(deftest dummy-test
  (is (= 1 1)))
