(ns ai.obney.grain.event-store-postgres-v2.interface-test
  (:require [clojure.test :as test :refer :all]
            [ai.obney.grain.event-store-postgres-v2.interface :as event-store-postgres-v2]))

(deftest dummy-test
  (is (= 1 1)))
